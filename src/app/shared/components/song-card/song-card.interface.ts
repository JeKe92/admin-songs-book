export interface ISongCard {
    title: string;
    artist: string;
    key?: string,
    tempo?: number,
    compass?: string,
    videoUrl?: string;
    lyricsUrl?: string;
    chordsUrl?: string;
};
